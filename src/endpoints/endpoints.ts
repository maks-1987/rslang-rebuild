// const URL = 'https://dt-lang-server.herokuapp.com'
const URL = 'https://rslang-be-rebuild-service.onrender.com'

const Endpoints = {
  ROOT: `${URL}`,
  USERS: `${URL}/users`,
  WORDS: `${URL}/words`,
  SIGNIN: `${URL}/signin`,
}

export default Endpoints
